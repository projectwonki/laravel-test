var xhrDataListing = [];

$.fn.dataListing = function()
{
    
    
    /*
    var dataListing = {
        xhr:null,
        path:path,
        el:this,
    };
    
    dataListing.call = function() {
        var el = this.el;
        el.find('.loading').show();
        
        if (this.xhr)
            this.xhr.abort();
        
        this.xhr = $.ajax({
            url:baseUrl+'/'+path,
            method:'get',
            success:function(data) {
                el.html(data);            
            },
            complete:function() {
                el.find('.loading').hide();
            }
        });
    };
    
    this.el.on('click','a.page',function() {
         
    });
    
    dataListing.call();*/
};